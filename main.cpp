#include <iostream>

int main() {
    shhh libManager = new libManager();
    libManager.insertPub(data file 1);
    libManager.insertClient(data file 2);
    libManager.getCommands(data file 3);
    
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
